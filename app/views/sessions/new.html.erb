<div class="auth-container">
  <div class="auth-card">
    <div class="auth-header">
      <h1>Welcome Back</h1>
      <p>Sign in to access the YIMBY LA Admin Portal</p>
    </div>

    <% if flash[:alert] %>
      <div class="alert alert-error">
        <%= flash[:alert] %>
      </div>
    <% end %>

    <%= form_with url: session_path, scope: :session do |form| %>
      <div class="form-group">
        <%= form.label :email_address, "Email" %>
        <%= form.email_field :email_address, 
            required: true, 
            autofocus: true,
            placeholder: "your@email.com",
            class: "auth-input" %>
      </div>

      <div class="form-group">
        <%= form.label :password %>
        <%= form.password_field :password, 
            required: true,
            placeholder: "Enter your password",
            class: "auth-input" %>
      </div>

      <%= form.submit "Sign In", class: "btn-auth" %>
    <% end %>

    <div class="auth-footer">
      <%= link_to "Forgot password?", new_password_path, class: "auth-link" %>
    </div>
  </div>

  <div class="public-link">
    <%= link_to "← Back to Public Form", root_path %>
  </div>
</div>